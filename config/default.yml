jwt:
  expiresIn: '1y' # default token expire time 1 year. Examples - https://github.com/vercel/ms#examples

logger:
  transports: [ 'console' ]

communication:
  services: [ ]

server:
  port: '8080'

grafana:
  labels:
    app: 'boilerplate-mern'
