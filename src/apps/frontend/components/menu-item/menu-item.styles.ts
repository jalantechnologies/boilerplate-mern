const styles = {
  menuItem: {
    wrapper: 'relative flex',
    dropdown:
      'absolute right-0 top-full z-40 w-40 space-y-1 rounded-sm border border-stroke bg-white p-1.5 shadow-default',
    dropdownVisible: 'block',
    dropdownHidden: 'hidden',
  },
};

export default styles;
