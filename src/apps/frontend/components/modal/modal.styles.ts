const styles = {
  modal: {
    overlay:
      'fixed left-0 top-0 z-99999 flex h-screen w-full justify-center overflow-y-scroll bg-black/80',
    overlayVisible: 'block',
    overlayHidden: 'hidden',
    container:
      'relative m-auto w-full max-w-180 rounded-sm border border-stroke bg-gray p-4 shadow-default sm:p-8 xl:p-10',
  },
};

export default styles;
