const styles = {
  select: {
    dropdown:
      'items-center justify-center rounded-lg border border-stroke bg-transparent px-2 py-4 text-lg outline-none focus:border-primary focus-visible:shadow-none',
  },
};

export default styles;
