const routes = {
  ABOUT: '/about',
  DASHBOARD: '/',
  LOGIN: '/login',
  OTP: '/login?auth_mode=otp',
  PHONE_LOGIN: '/signup?auth_mode=otp',
  SIGNUP: '/signup',
  TASKS: '/tasks',
};

export default routes;
