import { AccessToken } from '../types';

export default class AccessTokenWriter {
  public static async createAccessToken(params: CreateAccessTokenParams): Promise<AccessToken> {
    // TODO: Implement this
  }
}
