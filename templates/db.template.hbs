import { Schema, Types } from 'mongoose';

export interface {{pascalCase moduleName}}DB {
  _id: Types.ObjectId;
}

export const {{pascalCase moduleName}}DbSchema = new Schema<{{pascalCase moduleName}}DB>(
  {},
  {
    timestamps: {
      createdAt: 'createdAt',
      updatedAt: 'updatedAt',
    },
  }
);
