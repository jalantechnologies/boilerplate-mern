/* eslint-disable @typescript-eslint/no-misused-promises */
import { Router } from 'express';

export default class {{titleCase name}}Router {
  public static getRoutes(): Router {
    const router = Router();

    //router.post('/', {{name}}Controller.create{{titleCase name}});

    return router;
  }
}
